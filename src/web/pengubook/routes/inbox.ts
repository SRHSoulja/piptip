// src/web/pengubook/routes/inbox.ts - Inbox page handler (stub)
import { Request, Response } from "express";

export async function inboxHandler(req: Request, res: Response) {
  res.status(501).send("Inbox handler not yet implemented in modular structure");
}