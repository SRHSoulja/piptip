// src/web/pengubook/routes/profile.ts - Profile page handlers (stub)
import { Request, Response } from "express";

export async function profileHandler(req: Request, res: Response) {
  res.status(501).send("Profile handler not yet implemented in modular structure");
}

export async function profilePostHandler(req: Request, res: Response) {
  res.status(501).send("Profile POST handler not yet implemented in modular structure");
}