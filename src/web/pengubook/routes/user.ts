// src/web/pengubook/routes/user.ts - User profile and tipping handlers (stub)
import { Request, Response } from "express";

export async function userHandler(req: Request, res: Response) {
  res.status(501).send("User handler not yet implemented in modular structure");
}

export async function userTipHandler(req: Request, res: Response) {
  res.status(501).send("User tip handler not yet implemented in modular structure");
}