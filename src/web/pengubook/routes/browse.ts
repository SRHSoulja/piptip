// src/web/pengubook/routes/browse.ts - Browse users page handler (stub)
import { Request, Response } from "express";

export async function browseHandler(req: Request, res: Response) {
  res.status(501).send("Browse handler not yet implemented in modular structure");
}